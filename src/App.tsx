import React, { FC } from 'react';
import VideoUpload from './components/VideoUpload';

const App: FC = () => {
  return (
    <div>
      <h1>React Works</h1>
      <VideoUpload />
    </div>
  );
};

export default App;
